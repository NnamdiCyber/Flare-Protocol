/**
 * earnings.controller.js — Earner earnings history controller (stub).
 *
 * Full earnings history and claim UI is wired post Day 5.
 */

'use strict';

angular.module('flareApp.earner')
  .controller('EarningsCtrl', [
    '$scope',
    function ($scope) {
      $scope.stub = true;
    },
  ]);
